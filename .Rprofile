# R Profile for F1 Analytics Project

# Configure f1dataR to use built-in caching
# This enables automatic caching of API requests to the Ergast API
options(f1dataR.cache = "data/cache")

# Display message on startup
if (interactive()) {
  packageStartupMessage("f1dataR cache enabled: data/cache")
}
